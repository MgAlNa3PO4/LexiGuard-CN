# LexiGuard-CN —— 服务器部署依赖（CPU 版）
# Python 3.9+
# 安装：pip install -r requirements_server.txt

# ---------------------------------------------------------------------------
# 强制使用 CPU 版 PyTorch（不拉取任何 CUDA/cuDNN/GPU 运行库）
# ---------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu

# 向量化模型（CPU 版，中文检索：BAAI/bge-small-zh-v1.5）
sentence-transformers==2.7.0
transformers==4.49.0
huggingface-hub==0.31.2

# 本地向量数据库
chromadb==0.5.23

# 混合检索：BM25 关键词召回 + 中文分词
rank_bm25==0.2.2
jieba==0.42.1

# LangChain（业务依赖）
langchain==0.2.16

# 数值计算
numpy==1.26.4

# DeepSeek / OpenAI 兼容 API 调用
requests==2.32.3
openai==1.51.0

# 公众号文章抓取与解析
beautifulsoup4==4.12.2
lxml==4.9.3

# docx 文本提取与报告生成
python-docx==1.2.0

# 网页界面
gradio==4.44.1

# ---------------------------------------------------------------------------
# 关键传递依赖锁定（与本机可运行环境对齐）
# legacy-resolver 不回溯，会拉取超出 gradio 4.44.1 兼容上限的新版本，
# 其中 pydantic>=2.11 会触发 gradio_client "argument of type 'bool' is not iterable"
# 启动崩溃。以下版本与本机一致，确保容器内 Gradio 正常启动。
# ---------------------------------------------------------------------------
pydantic==2.10.1
fastapi==0.115.11
starlette==0.46.0
anyio==4.9.0
httpx==0.27.2
uvicorn==0.34.0
typer==0.15.3
pillow==10.4.0
websockets==12.0
tokenizers==0.21.0
